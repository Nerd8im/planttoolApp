import { StyleSheet } from "react-native"

const estiloDrawer = StyleSheet.create({
    containerLogo:{
        alignItems: 'center',
        padding: 20
    },
    logo:{
        width: 120,
        height: 60
    }
})

export default estiloDrawer